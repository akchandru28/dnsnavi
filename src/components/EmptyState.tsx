export default function EmptyState() {
  return (
    <div className="empty">
      <p>Run a test to see results.</p>
      <p className="form-hint">Tip: open Advanced options to include authoritative servers.</p>
    </div>
  );
}
